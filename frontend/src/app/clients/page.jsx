import StubPage from '@/components/StubPage';

export default function ClientsPage() {
  return (
    <StubPage
      icon="users"
      title="Client Management"
      blurb="Accounts, entitlements and contacts for every client. This service is planned — the pipelines console ships first."
    />
  );
}
