{
  "name": "admin-dashboard-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Admin dashboard API — Express, read-only over the Mongo pipeline ledger.",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
