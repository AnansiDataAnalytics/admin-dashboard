{
  "name": "admin-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Anansi internal admin dashboard — monorepo (Express backend + Next frontend).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "install:all": "npm install"
  }
}
