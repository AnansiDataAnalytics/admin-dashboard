import StubPage from '@/components/StubPage';

export default function AnalyticsPage() {
  return (
    <StubPage
      icon="chart"
      title="Usage Analytics"
      blurb="API calls, downloads and engagement across products. This service is planned — the pipelines console ships first."
    />
  );
}
